{
  "name": "Pith",
  "description": "High-performance CLI proxy that intercepts and compresses terminal outputs to save tokens for LLMs.",
  "version": "v0.5.6",
  "commands": {
    "start": "go run main.go dashboard",
    "install": "go mod download"
  },
  "networking": {
    "defaultPort": 8080,
    "healthCheck": "/"
  }
}
