# Local-only developer configuration. Pith does not load this file automatically.
# Copy to .env only when optional tooling requires these variables; never commit .env.
GEMINI_API_KEY=
THNEED_MASTER_DB=
